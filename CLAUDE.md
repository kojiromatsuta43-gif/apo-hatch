# アポハッチくん 開発ハンドオフ

このファイルは、このリポジトリで作業する人（と Claude Code）が最初に読むためのものです。
利用者向けの説明は `README.md` にあります。

## これは何か

問い合わせフォームとメールへの営業送信を自動化する Web アプリ。各自のパソコンにインストールして、各自の回線・各自のメールアカウントから送ります。サーバーは使いません。

**このリポジトリが開発の正であり、同時に配布元でもあります。** ここに push した内容が、配布済みの各PCの「アップデート」に流れます（`release.json` を更新したときだけ）。

似た機能が BRIDGE HATCH 本体（`create-works`）にも組み込まれていますが、**あちらは別のコード**です。片方を直しても、もう片方には反映されません。混同しないでください。

- リポジトリ: `github.com/kojiromatsuta43-gif/apo-hatch`（Public）
- 以前は `create-works` の `form-outreach/` に置き、ここへコピーを送る形でした。2026-09-10 に**このリポジトリ単独へ移行**しています（二重管理をやめるため）。

## 動かす

```bash
npm install                      # 初回。本体とは別に必要
npx playwright install chromium  # 初回。フォーム操作用のブラウザ本体
npm start                        # → http://localhost:3210
```

初回起動時に管理者アカウントがターミナルに1度だけ表示されます。`ADMIN_USER` / `ADMIN_PASSWORD` を環境変数で渡せば自分で決められます。
`npm start` は `scripts/run.mjs` 経由で、アップデート後の自動再起動（終了コード75）に対応します。`npm run dev` は素の起動で、再起動しません。

環境変数: `PORT`(3210) / `DATA_DIR`(./data) / `ANTHROPIC_API_KEY` or `GEMINI_API_KEY` / `CHROMIUM_PATH` / `HEADLESS=0`(ブラウザを見ながらデバッグ) / `COOKIE_SECURE`

## ファイルの地図

| ファイル | 役割 |
|---|---|
| `src/db.ts` | SQLite のスキーマとマイグレーション、型定義。`addCol()` で列追加、テーブル作り直しが要るときは `form_suppressions` の例を参照 |
| `src/auth.ts` | ログイン（scrypt + セッションCookie）、ユーザー発行。**単体版だけの機能**。本体組み込み時は不要 |
| `src/csv.ts` | 営業リストCSVと除外リストCSVの読み取り、キャンペーンへの登録と振り分け |
| `src/detect.ts` | 「営業お断り」文言と CAPTCHA の検知パターン |
| `src/formFinder.ts` | 会社URLから問い合わせフォームのページを探す |
| `src/formFiller.ts` | **中核。** 入力欄を集めて種類を判定し、値を入れて送信ボタンを押し、結果を判定する |
| `src/engine.ts` | ブラウザ起動、1社分の送信、事前チェック（`scanCompany`） |
| `src/message.ts` | 文面の組み立て（テンプレ／AI個別化／全文AI）、文面チェック（lint） |
| `src/email.ts` | SMTP送信、配信停止、SMTPエラーの日本語化 |
| `src/worker.ts` | キューを回す。送信時間帯・日次上限・リトライ・事前チェックの一括実行 |
| `src/views.ts` | 画面のHTML（テンプレートリテラル。フレームワークなし） |
| `src/server.ts` | Express のルーティング。認証・権限チェックもここ |
| `src/update.ts` | 配布後の自動アップデート |
| `test/e2e.ts` | ダミーサイト15パターンの通しテスト |

## 触る前に知っておくべきこと

### フォーム判定は経験則の積み重ね
`formFiller.ts` の `classify()` は、実際のサイトでハマった事例を1つずつ潰した結果です。うかつに整理すると再発します。特に:

- **自分のラベルを最優先**してから文脈を見る（`sei` を「姓」ではなく「性別」と誤判定した事故の対策）
- ラジオ・チェックボックスは**設問の見出し**から種類を判定する
- 数字のみの欄は `maxlength` / `pattern` / `inputmode` / `placeholder` から判定して、ハイフンを抜く
- `opacity:0` のカスタムチェックボックスは、ラベル側から可視判定する
- ハニーポット（隠し欄）には入力しない
- 送信結果の判定 `judgeOutcome()` は、**送信前のページテキストを渡して**「もともと完了文言があるページ」を誤判定しないようにしている

### 送信テストは絶対に自社サイトで
実在の企業に向けてテスト送信しないこと。`test/e2e.ts` はローカルに立てたダミーサイトに対して動くので、これで検証します。

```bash
npm test    # 2〜3分。送信まわりを直したら必ず流す
```

コンテナ等で Chromium のパスが通らない場合は `CHROMIUM_PATH=... npm test`。

### 安全装置は外さない
「営業お断り」検知、CAPTCHAスキップ、再送禁止期間、官公庁ドメイン除外、配信停止。いずれもキャンペーン設定で緩められますが、**既定値は安全側のまま**にしてください。クレームは配布元（松田さん）に返ってきます。

### データは触らない
`data/` には営業リスト・送信履歴・スクリーンショット・アカウントが入ります。`.gitignore` 済み。マイグレーションで**列の削除・リネームはしない**（追加はOK）。配布済みのPCで既存データが壊れます。

## 開発フォルダで「アップデート」ボタンを押さないこと

画面右上の「アップデート」は、**zipで配った利用者のための機能**です。GitHubから最新版をダウンロードして、フォルダの中のファイルを直接書き換えます。

このリポジトリを clone した開発フォルダでこれを押すと、gitから見て「勝手に書き換えられた状態」になり、次の `git pull` がこう言って止まります。

```
error: Your local changes to the following files would be overwritten by merge
```

**開発中の受け取りは必ず `git pull`** を使ってください。

もし押してしまったら、書き換えられた分を退避してから取り込みます（退避した中身は配布版と同じなので、捨てて構いません）。

```bash
git checkout -- .
git pull
npm start
```

動作確認のために「アップデート」ボタン自体を試したいときは、開発フォルダとは別の場所にzipを解凍して、そちらで試してください。

## リリース（配布済みPCに届ける）

push しただけでは誰にも届きません。**バージョンを上げて `release.json` を更新**して初めて、各PCの画面に「新しい版があります」と出ます。

```bash
git pull                               # 先に他の人の変更を取り込む
npx tsc --noEmit && npm test           # 型チェックと送信の通しテスト
npm version patch                      # 0.3.1 → 0.3.2
npm run release -- "直した内容を一言"     # ← この文言がそのまま利用者の画面に出る
git add -A && git commit -m "アポハッチくん v0.3.2" && git push
```

**`git push` は1回だけです。**（以前は配布用ミラーへの `git subtree push` も必要でしたが、リポジトリを一本化したので不要になりました）

**リリースする人が複数いるので、必ず順番を守ってください。**

1. 上のコマンド列は `git pull` から始まります。**途中だけコピーしないこと**（バージョン番号の取り合いを防ぐため）
2. 自分の環境で実際に画面を開いて、直した箇所を目で確認する
3. リリースしたら、チャットで一報を入れる（誰がどの版を出したか分かるように）

なお `npm version patch` は `package.json` の値を書き換えるだけです（このリポジトリでは git タグも作りますが、実害はありません）。

配布済みのPCに一斉に配られます。取り消しはできません（新しい版を出して上書きするしかない）。迷ったらリリースせず、`git push` だけで止めて相談してください。

**`update.json` を壊さないこと。** 配布済みの全PCがこのURLを見ています。空や `CHANGE-ME` を含む値で公開すると、以後どの端末も更新できなくなります（`update.ts` 側にガードあり）。

**バージョンを上げずに push した場合**は、コードは GitHub に入りますが誰にも配られません。開発中の細かい修正はそれでよく、区切りがついたところでリリースする、という使い分けで構いません。

## いま分かっている弱点・次にやるなら

1. **除外リストがPCごとに独立。** A社が断ってきても他のメンバーのアポハッチくんは知らない。共有する仕組みは未実装（サーバーを立てない方針とのトレードオフ）
2. **アプリパスワードが平文保存。** ローカル専用なので許容しているが、共有環境に置くなら暗号化が必要
3. **CIなし。** `npx tsc --noEmit` と `npm test` は手動
4. **フォーム判定の改善余地。** 「フォーム無し」「失敗」になったサイトの事例を集めて `formFinder.ts` / `formFiller.ts` を強くするのが、最も成果に直結する
5. **手動送信リスト**（CAPTCHA等で送れなかった分をCSV出力）はあるが、送信済みに戻す導線がない

## コードの作法

- 画面はテンプレートリテラルで組む。フレームワークやビルドは入れない（配布のハードルを上げないため）
- 依存パッケージを増やすときは慎重に。利用者の `npm install` が重くなる
- コメントは日本語。「なぜそうしているか」を書く（何をしているかはコードを読めば分かる）
- コミットメッセージは日本語1行 + 必要なら詳細
